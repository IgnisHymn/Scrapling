# 计划：数据保存到Excel

## 目标
将爬取的数据保存到Excel文件，而不是生成HTML报告。

## 输出文件
`\\192.168.110.40\胜加\自动化生产\TikTok运营端数据报表\DaysReports.xlsx`

## 实现方案

### 1. 修改 `config.py`
添加输出Excel路径配置：
```python
DAYS_REPORT_PATH = r"\\192.168.110.40\胜加\自动化生产\TikTok运营端数据报表\DaysReports.xlsx"
```

### 2. 重写 `report.py`
将 `ReportGenerator` 改为 `ExcelSaver`，功能：
- 如果sheet页不存在，创建新sheet并初始化表头
- 如果sheet页存在，在最后一行追加数据

### 3. 字段映射

| 爬取字段 | Excel列名 | 列位置 |
|---------|----------|--------|
| (日期) | Date | A |
| Ad requests | 广告请求 | B |
| Ad impressions | 广告曝光量 | C |
| Ad clicks | 广告点击量 | D |
| Ad click-through rate | 广告点击率 | E |
| eCPM | eCPM（总） | F |
| (空) | eCPM（US） | G |
| (空) | 广告收入（US） | H |
| Ad revenue | 广告收入（总） | I |
| (空) | 广告支出 | J |
| (空) | ROAS | K |
| (空) | IPU | L |
| Total users | 总用户数 | M |
| New users | 新用户数 | N |
| Active users | 活跃用户数DAU | O |
| Repeat users | 重复用户数 | P |
| Launched sessions | 启动次数 | Q |
| Average launched sessions | 人均启动次数 | R |
| Average duration per user | 每位用户的首日平均时长 | S |
| Average duration per session | 次均时长 | T |
| (空) | 次留 | U |
| (空) | 受众分布（性别） | V |
| (空) | 启动成功率 | W |
| (空) | 首次平均启动速度 | X |
| (空) | 平均启动速度 | Y |

### 4. 修改 `page_parser.py`
- 修改 `select_date_in_iframe` 方法，返回选择的日期字符串（YYYY-MM-DD格式）
- 在方法中解析日历中实际选择的日期并返回

### 5. 修改 `main.py`
- 删除HTML报告生成代码
- 删除 `report_gen` 相关代码
- 调用 `ExcelSaver` 保存数据到Excel
- 使用 `select_date_in_iframe` 返回的日期作为 Date 值

### 5. 删除文件
- 删除 `report.py`（或重写为Excel保存模块）

## 已确认
- Date格式：YYYY-MM-DD
- Date值：日历中选择的日期范围（不是固定的"前天"）
- 删除HTML报告生成功能
- 遍历 minisInfo.xlsx 中所有 app（每行一个 app_name + app_id）
- 每个 app_name 对应一个 sheet 页
- 每个 app都要爬取变现数据和仪表板数据
- 不存在的字段留空
- 所有 app 处理完成才算运行结束
